/logs/**
.idea/*
!.idea/codeStyles/
*.iml
/target

# build files
/build/
/dist/
/nbproject/
/.settings
/out
*.onetoc2

# Eclipse m2e generated files
# Eclipse Core
.project
# JDT-specific (Eclipse Java Development Tools)
.classpath

# Database
database/docker-db-data
database/docker-pg-db-data

# macOS files
.DS_Store

# Credentials - never commit API keys or tokens
*.env
*.key
*.key.txt

# Client binaries are Nexon IP and must never be committed to this repo.
# (Does not affect wz/**/*.img.xml, which is the server's own extracted data.)
*.exe
